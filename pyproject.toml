[project]
name = "privacyprov"
version = "0.1.0"
description = "Prototype workflow instrumentation layer for privacy-aware ML provenance"
requires-python = ">=3.10"
dependencies = ["pandas", "scikit-learn", "pyyaml"]

[tool.setuptools.packages.find]
where = ["."]
include = ["privacyprov*"]
